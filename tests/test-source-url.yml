name: Test source-url and owner inputs
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        with:
          dotnet-version: '8.0'
          source-url: https://nuget.pkg.github.com/${{ github.repository_owner }}/index.json
          owner: ${{ github.repository_owner }}
        env:
          NUGET_AUTH_TOKEN: ${{ github.token }}
      - name: Assert dotnet-version output is set
        env:
          ACTUAL: ${{ steps.action.outputs.dotnet-version }}
        run: |
          echo "actual: $ACTUAL"
          if [ -z "$ACTUAL" ]; then
            echo "FAIL: expected dotnet-version output to be set"
            exit 1
          fi
          echo "PASS: dotnet-version=$ACTUAL"
      - name: Assert dotnet is installed
        run: |
          dotnet --version
          echo "PASS: dotnet is installed"
